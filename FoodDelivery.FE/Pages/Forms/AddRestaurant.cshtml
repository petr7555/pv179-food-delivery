@page
@model AddRestaurant

@{
  ViewData["Title"] = "Add Restaurant";
}

<div class="row">
  <div class="col-md-4">

    <section>
      @using (Html.BeginForm())
      {
        <div class="form-group">
          @Html.LabelFor(x => x.Restaurant.Name, new { @class = "control-label" })
          @Html.TextBoxFor(x => x.Restaurant.Name, new { @class = "form-control" })
          @Html.ValidationMessageFor(x => x.Restaurant.Name, null, new { @class = "text-danger" })
        </div>
        <div class="form-group">
          @Html.LabelFor(x => x.Restaurant.DeliveryPriceId, new { @class = "control-label" })
          @Html.TextBoxFor(x => x.Restaurant.DeliveryPriceId, new { @class = "form-control" })
          @Html.ValidationMessageFor(x => x.Restaurant.DeliveryPriceId, null, new { @class = "text-danger" })
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary">Add</button>
        </div>
      }
    </section>
  </div>
</div>
